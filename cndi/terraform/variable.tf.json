{
  "variable": {
    "ARGOCD_ADMIN_PASSWORD": {
      "type": "string",
      "description": "ArgoCD admin password",
      "sensitive": true
    },
    "GIT_REPO": {
      "type": "string",
      "description": "Cluster git repository URL"
    },
    "SEALED_SECRETS_PUBLIC_KEY": {
      "type": "string",
      "description": "Sealed Secrets public key for encrypting secrets in the cluster"
    },
    "SEALED_SECRETS_PRIVATE_KEY": {
      "type": "string",
      "description": "Sealed Secrets private key for decrypting secrets in the cluster",
      "sensitive": true
    },
    "SSH_PUBLIC_KEY": {
      "description": "public key for accessing cluster nodes",
      "type": "string"
    },
    "GIT_TOKEN": {
      "type": "string",
      "description": "Git token for repository access",
      "sensitive": true
    },
    "GIT_USERNAME": {
      "type": "string",
      "description": "Git username for repository access"
    }
  }
}